#pragma once

#include <string>

namespace engine {

// Placeholder version identifier for the C++ port skeleton (Phase 1). This is
// separate from the Rust project's v2.2.0 versioning until the port reaches parity.
std::string version();

} // namespace engine
