// AiView.tsx
// Extracted from App.tsx (was an inline `<div className="ai-view" />` placeholder).
// No behavior change from the extraction itself -- this is where the AI tab's real
// functionality will grow once the AI direction (see docs/mds/AI.md) is decided.
export default function AiView() {
  return <div className="ai-view" />;
}
