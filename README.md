## Legacy Branch (Archived)

This branch preserves the original implementation of this project, written in **Rust**.

The project has since been fully rewritten and ported to **C++**, with substantial improvements to correctness, concurrency, and performance along the way.

**The `main` branch is the current, actively maintained version.** This `legacy` branch is kept only as a historical reference and is no longer developed.
